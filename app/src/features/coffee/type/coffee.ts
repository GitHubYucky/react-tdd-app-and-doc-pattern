export type CoffeeType = {
    title: string;
    description: string;
    ingredients: string[];
    image: string;
    id: number;
  };
  export type CoffeeApiType = {
    title: string;
    description: string;
    ingredients: string[]|string;
    image: string;
    id: number;
  };
